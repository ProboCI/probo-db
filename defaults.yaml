host: 0.0.0.0
port: 8442

buildsEventStream:
  plugin: Kafka
  config:
    groupId: 'proboDb'
    consumerTopic: 'build_events'
    brokers:
      - 'localhost:9092'
    clientId: 'probo-db'
    version: 1

pluginDirectory: null

knex: {}

serverName: probodb

version: '1.0.0'
