---
buildsEventStream:
  plugin: Kafka
  config:
    group: 'proboDb'
    topic: 'build_events'
    version: 1
pluginDirectory: null
knex: {}
port: 8442
host: localhost
serverName: probodb
version: '1.0.0'
