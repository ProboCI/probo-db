host: 0.0.0.0
port: 8442

buildsEventStream:
  plugin: Kafka
  config:
    group: 'proboDb'
    topic: 'build_events'
    version: 1
pluginDirectory: null

knex: {}

serverName: probodb

version: '1.0.0'
